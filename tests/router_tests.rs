#[test]
fn placeholder() {
    // Place holder test
    // Should move main.rs logic to lib.rs
    // Then start up a webserver, maybe a virtual one
    // Possible use Reqwuest to call the API
    // assert!(true);
}
